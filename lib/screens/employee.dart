import 'package:flutter/material.dart';

class EmployeeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Employee")),
      body: Center(child: Text("Employee Page")),
    );
  }
}
