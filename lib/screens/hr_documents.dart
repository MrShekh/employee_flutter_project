import 'package:flutter/material.dart';

class HRDocumentsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("HR Documents")),
      body: Center(child: Text("HR Documents Page")),
    );
  }
}
