import 'package:flutter/material.dart';

class TaskBoxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Task Box")),
      body: Center(child: Text("Task Box Page")),
    );
  }
}
