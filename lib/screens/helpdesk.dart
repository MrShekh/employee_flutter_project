import 'package:flutter/material.dart';

class HelpdeskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Helpdesk")),
      body: Center(child: Text("Helpdesk Page")),
    );
  }
}
