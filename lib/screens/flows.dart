import 'package:flutter/material.dart';

class FlowsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Flows")),
      body: Center(child: Text("Flows Page")),
    );
  }
}
